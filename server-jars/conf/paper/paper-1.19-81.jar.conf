## See the default craftbukkit.jar.conf for a detailed documentation of the
## format of this file.
[config]
name = paper-1.19-81
source = https://api.mymultihosting.com/server-jars/paper/paper-1.19-81.jar
configSource = https://api.mymultihosting.com/server-jars/conf/paper-1.19-81.jar.conf
category = Paper MC

[encoding]
#encode = system
#decode = system
#fileEncoding = latin-1

[start]
command = "{JAVA}" -Xmx{MAX_MEMORY}M -Xms{START_MEMORY}M -Djline.terminal=jline.UnsupportedTerminal -jar "{JAR}" nogui

[parse_players]
start2=^There are \d+ of a max of \d+ players online:\s*(?P<v_listStr_append>.*)

[parse_chat]
start=^(?:\[Not Secure\]\s)?(?P<source>\[[^\]]+\])?\s*<(§.)?(?P<sender>[^§>]*)(§.)?>\s*(?P<message>.*)$
